import { SpellCasttimePipe } from './spell-casttime.pipe';

describe('SpellCasttimePipe', () => {
  it('create an instance', () => {
    const pipe = new SpellCasttimePipe();
    expect(pipe).toBeTruthy();
  });
});
