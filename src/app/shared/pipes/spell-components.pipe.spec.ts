import { SpellComponentsPipe } from './spell-components.pipe';

describe('SpellComponentsPipe', () => {
  it('create an instance', () => {
    const pipe = new SpellComponentsPipe();
    expect(pipe).toBeTruthy();
  });
});
