import { SpellLevelDisplayPipe } from './spell-level-display.pipe';

describe('SpellLevelDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new SpellLevelDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
