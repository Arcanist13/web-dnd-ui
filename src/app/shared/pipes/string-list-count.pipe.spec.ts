import { StringListCountPipe } from './string-list-count.pipe';

describe('StringListCountPipe', () => {
  it('create an instance', () => {
    const pipe = new StringListCountPipe();
    expect(pipe).toBeTruthy();
  });
});
